
@{
    ViewBag.Title = "Index";
}

<h2>喝水建議</h2>
@using (Html.BeginForm("Index", "Water", FormMethod.Get))
            {
                <p>姓名 : @Html.TextBox("name")</p>
                <p>體重 : @Html.TextBox("w") </p>
                <p>hi @ViewBag.name, 建議喝 @ViewBag.result</p>
                <input type="submit" value="送出" />
            }

